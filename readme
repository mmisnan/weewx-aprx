weewx-aprx - APRX ready output file

weewx-aprx is an extension for weewx which produces a file which can be
feed directly into aprx. The code is based on the cwxn (Cumulus WXNow) 
interface by Matthew Wall.

The format is following the APRS Weather format which is available from:

http://www.aprs.net/vm/DOS/PROTOCOL.HTM

Installation instructions:

1) run the installer (v5)

weectl extension install weewx-aprx.tgz

2) restart weewx

sudo systemctl stop weewx
sudo systemctl start weewx
